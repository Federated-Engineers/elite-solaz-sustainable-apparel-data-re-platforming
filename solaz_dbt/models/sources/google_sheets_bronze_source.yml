version: 2

sources:
  - name: google_sheets
    description: Orders data ingested from Google Sheets into Snowflake bronze schema.

    database: SOLAZ_PROD_DB
    schema: BRONZE

    quoting:
      database: false
      schema: false
      identifier: false

    tables:
      - name: orders
        description: Transaction records from Google Sheets.